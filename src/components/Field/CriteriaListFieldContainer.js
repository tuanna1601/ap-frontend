import { connect } from 'react-redux';

import CriteriaListField from './CriteriaListField';

const mapStateToProps = () => ({});

const mapDispatchToProps = () => ({});

export default connect(mapStateToProps, mapDispatchToProps)(CriteriaListField);
